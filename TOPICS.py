# ============================================================
#              PYTHON_WORKS - TOPICS OVERVIEW
# ============================================================
# A complete reference of every topic covered in this workspace.
# Each section maps to a folder and its key concepts.
# ============================================================


# ─────────────────────────────────────────────────────────────
# 01_Basics
# ─────────────────────────────────────────────────────────────
# Calculations/         → Arithmetic ops, average, bill cost, BMI, BMR, Simple Interest
# Conversions/          → Temp (°C ↔ °F), Years → Days, Seconds → Days/Hours/Mins/Secs
# IO_and_Variables/     → input(), print(), type casting, f-strings, variable assignment
# Operators/            → Arithmetic, Comparison, Assignment, Logical, Bitwise, Math module


# ─────────────────────────────────────────────────────────────
# 02_Decision_Making
# ─────────────────────────────────────────────────────────────
# If_Else/              → if / elif / else branching
# Logical_Operators/    → and, or, not in conditions
# Nested_If/            → if inside another if
# Ternary_Operator/     → value_if_true if condition else value_if_false


# ─────────────────────────────────────────────────────────────
# 03_While_Loops
# ─────────────────────────────────────────────────────────────
# Basics/               → while condition: ... , break, continue
# Number_Problems/      → digit sum, reverse digits, count digits
# Special_Numbers/      → Armstrong, palindrome, prime via while loop


# ─────────────────────────────────────────────────────────────
# 04_For_Loops
# ─────────────────────────────────────────────────────────────
# Basics/               → for i in range(), iterating lists/strings
# Number_Problems/      → sum of n, multiplication table, FizzBuzz, factorial
# Range_Programs/       → range(start, stop, step) patterns
# Special_Numbers/      → perfect number, prime, Armstrong via for loop


# ─────────────────────────────────────────────────────────────
# 05_Strings
# ─────────────────────────────────────────────────────────────
# Basics/               → Slicing s[start:stop:step], s[::-1] (reverse), count vowels
# Case_and_Characters/  → lower(), upper(), title(), swapcase(), palindrome, ASCII ord()/chr()
# Frequency_and_Search/ → Character frequency, Counter, first repeating, find(), count()


# ─────────────────────────────────────────────────────────────
# 06_Patterns
# ─────────────────────────────────────────────────────────────
# star_patterns         → Right triangle, inverted triangle, pyramid, diamond, rectangle, hollow rectangle
# exam_pattern          → Practice patterns using nested loops


# ─────────────────────────────────────────────────────────────
# 07_Functions
# ─────────────────────────────────────────────────────────────
# Basics/               → def func(), parameters, return values, default args
# Args_and_Kwargs/      → *args (tuple), **kwargs (dict), argument unpacking
# Math_Functions/       → Prime, Armstrong, Perfect number using functions
# Recursion/            → Base case, recursive case, factorial, Fibonacci, power, sum_n
# String_Functions/     → Anagram, Pangram, remove duplicates, reverse before char


# ─────────────────────────────────────────────────────────────
# 08_Collections
# ─────────────────────────────────────────────────────────────
# Dictionaries/         → Key-value pairs, nested dicts, dict methods (.get, .keys, .values, .items)
# List_Comprehension/   → [expr for x in iterable if cond], [expr1 if cond else expr2 for x in iterable]
# List_Operations/      → Append, extend, insert, pop, remove, sort, reverse, count, max, min, frequency
# Searching_and_Sorting/→ Linear search, binary search, largest/smallest, 2nd largest, missing number


# ─────────────────────────────────────────────────────────────
# 09_Tasks
# ─────────────────────────────────────────────────────────────
# Date-wise practice sets covering combined python problems and exercises.


# ─────────────────────────────────────────────────────────────
# 10_Exception_Handling
# ─────────────────────────────────────────────────────────────
# exception_handling_intro      → try, except, else, finally blocks
# multiple_exceptions_handling  → Handling ValueError, ZeroDivisionError, TypeError separately
# dict_key_error_handling       → Handling KeyError safely using try-except or .get()
# list_index_error_handling     → Handling IndexError when accessing invalid list indices
# custom_exception_raise        → Raising exceptions manually using `raise Exception("msg")`
# assert_error_handling         → Using `assert condition, "Error Message"` for debugging assumptions


# ─────────────────────────────────────────────────────────────
# 11_File_Handling
# ─────────────────────────────────────────────────────────────
# file_handling_intro           → open(filename, mode), mode options ('r', 'w', 'a', 'b')
# file_read_methods             → .read(), .readline(), .readlines()
# file_write_mode               → Writing to file using 'w' mode (overwrites content)
# file_append_mode              → Appending text to file using 'a' mode
# file_with_statement           → Using `with open(...) as f:` context manager (auto file closing)
# file_read_exception           → FileNotFoundError handling when opening non-existent files


# ─────────────────────────────────────────────────────────────
# 12_OOPs (Object-Oriented Programming System)
# ─────────────────────────────────────────────────────────────
# Basics/                       → Class, Object, __init__() constructor, self, instance methods & attributes
# Inheritance/                  → Single inheritance, Multilevel inheritance, Multiple inheritance, super()
# Polymorphism/                 → Method Overriding, Method Overloading (default arguments / *args)
# Abstraction/                  → Abstract Base Class (ABC), @abstractmethod from `abc` module
